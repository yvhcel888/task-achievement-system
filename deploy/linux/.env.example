# 任务成就系统 - Linux 环境变量模板
# 复制为 .env 后修改，chmod 600 .env 保护密码

# 监听端口（默认 8889）
PORT=8889

# MySQL 连接（Ubuntu 24.04 自带 MySQL 8.0.46）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD='<your-db-password>'
DB_NAME=rxy
